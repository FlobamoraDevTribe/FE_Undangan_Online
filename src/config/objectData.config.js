export const objectMapData = (id = '', title = '') => ({ id, title })

export const objectBottomNavbar = (link = '', icon = '', isHome = false) => ({
   link,
   icon,
   isHome,
})

export const objectImageGallery = (src = '', description = '') => ({
   src,
   description,
})
